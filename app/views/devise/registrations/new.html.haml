.signup.mainfont
  .signup__main.box-center.background-white
    = render "shared/content_top"
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      .user-detail
        .user-detail__test
          テストアカウントがございますので、登録前にお試しいただけます。
          %br
          ログインにお進みください。
        - if devise_error_messages!.present?
          .signup__main__alert
            = devise_error_messages!
        .user-detail__department_id
          = f.hidden_field :department_id, value: "1", type: "hidden"
        .user-detail__top.form-content
          名前
        .user-detail__name-input
          = f.text_field :name, placeholder: ">name", autofocus: true, autocomplete: "name", class: 'text-input'
        .user-detail__top.form-content
          メールアドレス
        .user-detail__email-input
          = f.email_field :email, placeholder: ">e-mail", autofocus: true, autocomplete: "email", class: 'text-input'
        .user-detail__top.form-content
          パスワード(最低#{@minimum_password_length}文字以上)
        .user-detail__password-input
          - if @minimum_password_length
            = f.password_field :password, placeholder: ">password", autocomplete: "new-password", class: 'text-input'
        .user-detail__top.form-content
          パスワード（確認用）
        .user-detail__password-input
          = f.password_field :password_confirmation, placeholder: ">password", autocomplete: "new-password", class: 'text-input'
        .btn-box.display-flex
          .btn-box__user-new.box-center
            = f.submit " 新規登録 ", class: "submit-btn text-line-none"
          .btn-box__back.box-center
            = link_to new_user_registration_path, class: "blue-white-btn" do
              ログイン
        .explanation.text-gray
          既にアカウントをお持ちの方はログイン画面へお進みください。
 